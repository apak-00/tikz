\begin{tikzpicture}

	\tikzset{
		>=stealth',
		frame/.style={ rectangle, rounded corners, draw=black, thick, text width=6.5em,
			minimum height=2em, text centered}
	}
	
	%nodes
	\def\sep{1.15em};
	\def\rsep{4.8em};
	
	\node[frame] (horizon) {Local (Topocentric Horizon)};
	\node[below=\sep of horizon] (dummy1) {};
	\node[text width=8em, minimum height=2em, right = \rsep of dummy1] (tf1) {\small{latitude, longitude, height, azimuth, elevation, range}};
	
	\node[frame, inner sep=5pt,below=\sep of dummy1] (earth) {Earth-Fixed (ECEF) } edge[<->] (horizon.south);
	\node[below=\sep of earth] (dummy2) {};
	\node[text width=8em, minimum height=2em, right = \rsep of dummy2] (tf2) {\small{JDN, epoch}};
	
	\node[frame, inner sep=5pt, below=\sep of dummy2] (space) {Space-Fixed (ECI)} edge[<->] (earth.south);

\end{tikzpicture}